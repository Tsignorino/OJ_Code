# pyproject.toml

[tool.black]
line-length = 120

[tool.isort]
line_length = 120

skip = ["tests", "setup.py"]
skip_glob = ["*.pyc", "__pycache__"]

quiet = true
from_first = true
order_by_type = true
